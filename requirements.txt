# ComfyUI-DiscordSend Dependencies
# 
# For nodes only (minimal): pip install -r requirements-nodes.txt
# For full bot support:     pip install -r requirements-bot.txt
#
# This file includes all dependencies (bot + nodes)
-r requirements-bot.txt